// Command replaygate is the CLI entrypoint; subcommands are added incrementally per milestone (see TRD_Replay_Gate.md).
package main

import "fmt"

func main() {
	fmt.Println("replaygate: not yet implemented")
}
